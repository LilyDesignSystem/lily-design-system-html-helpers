---
layout: layout.njk
title: Home — Lily Theme Chooser
---

<h1>{{ title }}</h1>

<p>Theme chooser is mounted in the layout. Build-time default:
<strong>{{ theme.defaultTheme }}</strong>.</p>

<p>Available themes: <code>{{ theme.available | join(", ") }}</code>.</p>
